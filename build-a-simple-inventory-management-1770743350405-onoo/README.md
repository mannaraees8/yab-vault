# Build A Simple Inventory Management 1770743350405 Onoo

Generated by YAB (Your App Builder)

## Description
Build a simple inventory management app with images:

FIELDS: item name (text, required), description (text), quantity (number, required), category (select: electronics/furniture/clothing/other), image (file upload, required).

VIEWS: List view showing all inventory items with images, create/edit form for adding/editing items.

FEATURES: Add, edit, delete inventory items. Simple search by item name. Sort by category or quantity. Display images for each item.

## Quick Start

### Option 1: Single File (Easiest)
1. Open `index.html` directly in your browser
2. Start using the app immediately - no setup required!

### Option 2: Separate Files (For Development)
1. Open `index-split.html` in your browser
2. Make sure `styles.css` and `app.js` are in the same folder

## Data Storage

**Default:** Your data is stored locally in your browser (localStorage)
- Works immediately, no configuration needed
- Data persists between sessions
- Private to your browser

**Optional GitHub Backup:**
1. Go to Settings in the app
2. Enter your GitHub personal access token
3. Enter your GitHub username
4. Data will automatically sync to: `your-username/yab-vault/build-a-simple-inventory-management-1770743350405-onoo/data.json`

## Features
- ✅ Full CRUD operations (Create, Read, Update, Delete)
- ✅ Local data persistence (localStorage)
- ✅ Optional GitHub sync for backup
- ✅ Responsive mobile-first design
- ✅ Dark mode support
- ✅ Clean, modern UI

## Tech Stack
- **Frontend:** Vanilla JavaScript (ES6+)
- **Styling:** Tailwind CSS (CDN)
- **Icons:** Lucide Icons
- **Storage:** localStorage + GitHub API (optional)
- **Routing:** Hash-based navigation

## File Structure
```
build-a-simple-inventory-management-1770743350405-onoo/
├── index.html          # Single-file version (recommended)
├── index-split.html    # HTML structure only
├── styles.css          # Separated CSS
├── app.js              # Separated JavaScript
├── data.json           # Empty data file
└── README.md           # This file
```

## Privacy & Security
- All data stored locally in your browser first
- GitHub sync is completely optional
- No external servers or tracking
- You own 100% of your data

---

Built with ❤️ by [YAB - Your App Builder](https://github.com/yourusername/yab)
